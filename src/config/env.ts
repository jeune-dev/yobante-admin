// ─────────────────────────────────────────────────────────────
// config/env.ts — Variables d'environnement typées et validées
// ─────────────────────────────────────────────────────────────

// TODO: Valider avec Zod au démarrage
//   const envSchema = z.object({
//     VITE_SHOP_API_URL: z.string().url(),
//     VITE_SHIPMENT_API_URL: z.string().url(),
//     VITE_APP_NAME: z.string().default('Yobante Admin'),
//   })
// TODO: export const ENV = envSchema.parse(import.meta.env)
// TODO: Lever une erreur claire si une variable est manquante
