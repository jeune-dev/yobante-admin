// shared/types/api.types.ts
// TODO: interface PaginatedResponse<T> { rows: T[], count: number, totalPages: number, currentPage: number }
// TODO: interface ApiError { message: string, errors?: Record<string, string[]>, statusCode: number }
// TODO: type SortOrder = 'asc' | 'desc'
// TODO: interface PaginationParams { page?: number, limit?: number, sortBy?: string, sortOrder?: SortOrder }
