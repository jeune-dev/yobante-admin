// auth.service.ts
// TODO: login(payload) — appelle shopClient.post('/auth/login') ET shipmentClient.post('/auth/login') en parallèle (Promise.all)
// TODO: logout() — invalide les 2 tokens via API + clearAll() tokenManager
// TODO: refreshTokens() — renouvelle les 2 access tokens
