// ─────────────────────────────────────────────────────────────
// auth/hooks/useAuth.ts
// ─────────────────────────────────────────────────────────────

// TODO: Hook qui consomme authStore et expose une API propre
//   const { user, isAuthenticated, selectedApp, logout, selectApp } = useAuth()

// TODO: selectApp(app: 'shop' | 'shipment'): void
//   - authStore.setSelectedApp(app)
//   - navigate vers /shop/dashboard ou /shipment/dashboard selon le choix
