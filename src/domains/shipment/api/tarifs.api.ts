// domains/shipment/api/tarifs.api.ts
// TODO: getTarifs(filters): Promise<Tarif[]>                    -> GET /admin/tarifs
// TODO: createTarif(data): Promise<Tarif>                       -> POST /admin/tarifs
// TODO: updateTarif(id, data): Promise<Tarif>                   -> PUT /admin/tarifs/:id
// TODO: deleteTarif(id): Promise<void>                          -> DELETE /admin/tarifs/:id
// TODO: calculerPrix(data: CalculPrixRequest): Promise<CalculPrixResponse> -> POST /admin/tarifs/calculer-prix
