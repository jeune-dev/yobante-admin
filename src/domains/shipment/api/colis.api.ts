// colis.api.ts — Appels shipmentClient pour les colis
// TODO: getColis(filters) / getColisById(id) / createColis(data) / updateColis(id,data) / updateStatut(id,data) / ajouterPhotos(id,files) / exportColis(filters)
