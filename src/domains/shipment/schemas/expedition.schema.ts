// domains/shipment/schemas/expedition.schema.ts
// TODO: createExpeditionSchema = z.object({
//   colisIds: z.array(z.string().uuid()).min(1),
//   adresseId: z.number().int().positive(),
//   typeExpedition: z.enum(['standard', 'express']),
//   note: z.string().max(500).optional(),
// })
// TODO: marquerEnTransitSchema = z.object({ transporteur: z.string().min(2), numeroSuivi: z.string().min(3) })
