// domains/shipment/types/tarif.types.ts
// TODO: interface Tarif { id: number, zoneId: number, typeColis: ColisType, poidsMin: number, poidsMax: number, prixParKg: number, prixFixe: number, delaiEstime: number, zone?: Zone }
// TODO: interface Zone { id: number, nom: string, pays?: Pays[] }
// TODO: interface Pays { id: number, nom: string, code: string, drapeau?: string }
// TODO: interface CalculPrixRequest { poids: number, zoneId: number, typeColis: ColisType }
// TODO: interface CalculPrixResponse { tarifApplique: Tarif, montantTotal: number, delaiEstime: number }
