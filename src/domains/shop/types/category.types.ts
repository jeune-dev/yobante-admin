// domains/shop/types/category.types.ts
// TODO: interface Category { id: number, nom: string, slug: string, description?: string, image?: string, isActive: boolean, parentId?: number, sousCategories?: Category[] }
// TODO: interface CreateCategoryDTO { nom: string, description?: string, parentId?: number }
