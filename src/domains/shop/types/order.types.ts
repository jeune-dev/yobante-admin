// domains/shop/types/order.types.ts
// TODO: type OrderStatus = 'en_attente' | 'validee' | 'en_preparation' | 'expediee' | 'livree' | 'annulee'
// TODO: interface OrderItem { id: number, produitId: string, quantite: number, prixUnitaire: number, sousTotal: number, produit?: Product }
// TODO: interface Order { id: string, reference: string, userId: string, statut: OrderStatus, montantTotal: number, fraisLivraison: number, note?: string, noteAdmin?: string, items?: OrderItem[], user?: ShopUser, adresse?: any }
// TODO: interface OrderFilters { statut?: OrderStatus, userId?: string, dateDebut?: string, dateFin?: string, page?: number, limit?: number }
