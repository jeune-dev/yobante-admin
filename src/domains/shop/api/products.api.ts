// products.api.ts — Appels Axios shopClient pour les produits
// TODO: getProducts(filters) / getProductById(id) / createProduct(data) / updateProduct(id,data) / deleteProduct(id) / toggleFeatured(id) / updateStock(id,qty)
